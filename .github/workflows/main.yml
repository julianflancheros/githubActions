name: main-testbuild

on: [push]

jobs: 
  say-hello:
    runs-on: ubuntu-latest
    
    env:
      MY_VAR: "Mi variable de entorno"

    steps: 
      - name: Checkout
        uses: actions/checkout@v2

      - name: Test
        run: echo "Valor $MY_VAR"

